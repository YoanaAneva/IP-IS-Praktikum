#include <iostream>
# include <cmath>
using namespace std;
int main(){

    // a)

    // int number;
    // cout << "number: ";
    // cin >> number;
    // bool isTrue = (number % 4 == 0) || (number % 7 == 0);
    // (isTrue ? true : false);
    // cout << boolalpha << isTrue << endl;

    // б)

    // double a, b, c;
    // cout << "a.x2 + b.x + c = 0 (a != 0)" << endl << "a= ";
    // cin >> a;
    // cout << "b= ";
    // cin >> b;
    // cout << "c= ";
    // cin >> c;
    // double D = b * b - 4 * a * c;

    // bool hasNoRoots = D < 0;
    // hasNoRoots ? true : false;
    // cout << boolalpha << hasNoRoots << endl;

    // // в)

    // int a, b;
    // cout << "a: ";
    // cin >> a;
    // cout << "b: ";
    // cin >> b;

    // bool isInside = sqrt((a - 0) * (a - 0) + (b - 1) * (b - 1)) < 5;
    // isInside ? true : false;

    // cout << boolalpha << isInside << endl;

    // г)

    // int a, b, c, d, f;
    // cout << "A coordinates: " << endl <<  "a: ";
    // cin >> a;
    // cout << "b: ";
    // cin >> b;
    // cout << "Center coordinates: " << endl << "c: ";
    // cin >> c;
    // cout << "d: ";
    // cin >> d;
    // cout << "Radius: ";
    // cin >> f;

    // bool isInside = sqrt((a - c) * (a - c) + (b - d) * (b - d)) < f;
    // isInside ? true : false;

    // cout << boolalpha << isInside << endl;

    // д)

    // int a, b;
    // cin >> a >> b;

    // bool is_in_3rd = (sqrt((a - 0) * (a - 0) + (b - 0) * (b - 0)) < 5) && a <= 0 && b <= 0;
    // cout << boolalpha << is_in_3rd << endl;

    // е)

    // int a, b;
    // cin >> a, 
    // cin >> b;

    // bool isInside = (sqrt((a - 0) * (a - 0) + (b - 0) * (b - 0))) > 5 &&
    //                 (sqrt((a - 0) * (a - 0) + (b - 0) * (b - 0))) < 10 ;
    // isInside ? true : false;

    // cout << boolalpha << isInside << endl;
   
    // ж)

    // double x;
    // cout << "x: ";
    // cin >> x;

    // bool isInSec = x >= 0 && x <= 1;
    // isInSec ? true : false;
    // cout << boolalpha << isInSec << endl;

    // з)

    // int a, b, c, x;
    // cin >> a;
    // cin >> b;
    // cin >> c;
    // cout << "x: ";
    // cin >> x;

    // int biggest = a;
    // if (b > a)
    // {
    //     biggest = b;
    // }
    // if (c > biggest)
    // {
    //     biggest = c;
    // }
    // bool isMax = x == biggest;
    // isMax ? true : false;
    // cout << boolalpha << isMax << endl;

    // и)

    // isMax ? false : true;
    // cout << boolalpha << isMax << endl;

    // к)

    // bool x;
    // cin >> x;
    // bool y;
    // cin >> y;
    // bool areTrue = x || y;
    // areTrue ? true : false;
    // cout << boolalpha << areTrue << endl;

    // л)

    //  bool x;
    // cin >> x;
    // bool y;
    // cin >> y;
    // bool areTrue = x && y;
    // areTrue ? true : false;
    // cout << boolalpha << areTrue << endl;

    // м)

    // int a, b, c;
    // cin >> a >> b >> c;
    // bool arePositive = a < 0 && b < 0 && c < 0;
    // arePositive ? true : false;
    // cout << boolalpha << arePositive << endl;

    // н)

    // int p, first, second, third;
    // cin >> p;
    // first = p % 10;
    // second = (p / 10) % 10;
    // third = p / 100;
    // bool isSeven = first == 7 || second == 7 || third == 7;
    // isSeven ? true : false;
    // cout << boolalpha << isSeven << endl;

    // о)
    // int m, first, second, third;
    // cin >> m;
    // first = m % 10;
    // second = (m / 10) % 10;
    // third = m / 100;
    // bool areDifferent = first != second && second != third && third != first;
    // areDifferent ? true : false;
    // cout << boolalpha << areDifferent << endl;

    // п)

    // int m, first, second, third;
    // cin >> m;
    // first = m % 10;
    // second = (m / 10) % 10;
    // third = m / 100;
    // bool areSame = first == second || second == third || third == first;
    // areSame ? true : false;
    // cout << boolalpha << areSame << endl;

    return 0;

}